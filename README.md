
SyncBoard is a static Kanban task board front end where teams organize tasks across To Do, Doing and Done columns.

```bash
cd client
npm install
npm run dev
```

URL: http://localhost:5173 — sign-in at `/`, board at `/board`, profile at `/profile`.

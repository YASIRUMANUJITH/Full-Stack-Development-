server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback — deep links like /boards/<id> must serve index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
